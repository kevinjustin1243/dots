# Dots

Contains:
- DWL build
- somebar build
- colors from wallpaper with pywal16
- `.config` files for `foot` and `wofi`

External Software Requirements:
- `foot`
- `wbg`
- `wofi`
- `pipewire`, `pipewire-pulse`, `wireplumber`
- build tools specified by `dwl` and `somebar`
